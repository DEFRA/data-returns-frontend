//var config = require('./app/config/configuration_' + (process.env.NODE_ENV || 'local'));

//var json = JSON.stringify(config, null, 4);

//var fs = require('fs');
//fs.writeFile("./app/config/configuration_" + (process.env.NODE_ENV || "local") + ".json", json, function(err) {
//    if(err) {
//        return console.log(err);
//    }

//    console.log("The file was saved!");
//});

